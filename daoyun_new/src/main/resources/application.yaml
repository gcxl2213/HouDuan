spring:
  datasource:
    url: jdbc:mysql://localhost:3306/daoyun
    username: root
    password: 1021onlyONE
    driver-class-name: com.mysql.cj.jdbc.Driver
    redis:
      #redis 服务器地址
      host: 127.0.0.1
      #redis服务器连接端口
      port: 6379
server:
  port: 8080
logging:
  level:
    root: info
    com.baizhi.dao: debug

